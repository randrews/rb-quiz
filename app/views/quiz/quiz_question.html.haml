.question-text=@current_question.question

%ul.answers
  -@current_question.answers.each do |answer|
    %li.answer=link_to answer.answer, answer_quiz_url(@quiz.short_name, @current_question.id, answer.id)